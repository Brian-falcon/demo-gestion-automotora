{
  "version": 2,
  "buildCommand": "npm install --prefix frontend && npm run build --prefix frontend && npm install --prefix api && npm install --prefix backend && npx prisma generate --schema=backend/prisma/schema.prisma",
  "outputDirectory": "frontend/dist",
  "functions": {
    "api/index.js": {
      "runtime": "nodejs20.x",
      "includeFiles": "backend/**"
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index"
    }
  ]
}
