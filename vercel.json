{
  "version": 2,
  "buildCommand": "cd frontend && npm install && npm run build && cd ../api && npm install && cd ../backend && npx prisma generate",
  "outputDirectory": "frontend/dist",
  "functions": {
    "api/index.js": {
      "runtime": "nodejs20.x",
      "includeFiles": "backend/**"
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index"
    }
  ]
}
